# Server Configuration
# Port number for the server to run on
PORT=8080

# MongoDB Configuration
# For MongoDB Atlas (cloud database), use format:
# mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority&appName=app-name
# For local MongoDB, use: mongodb://localhost:27017/audio-library
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-database?retryWrites=true&w=majority&appName=your-app-name

# JWT Configuration
# Generate a secure random string (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-64-character-random-secret-key-replace-this-with-actual-secure-key